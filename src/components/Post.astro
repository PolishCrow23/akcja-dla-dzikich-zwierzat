---
import { type CollectionEntry } from "astro:content";
import FormattedDate from "./FormattedDate.astro";

type Props = {
    post: CollectionEntry<"posts">
}

const { post } = Astro.props
---

<div class="post-card">
    <a href={`/blog/${post.slug}`}>
        <img 
            src ={post.data.image}
            width={312}
            height={232}
            alt={post.data.title}
        />
    </a>
    <div class="title-post">
        <a href={`/blog/${post.slug}`}>
            <h2 class="title">
                {post.data.title}
            </h2>
        </a>
        <a href={`/blog/${post.slug}`}>
            <h2 class="date">
                <FormattedDate date={post.data.date}>
            </h2>
        </a>
    </div>
    <p>
        {post.body}
    </p>
    <a class="show-more" href={`/blog/${post.slug}`}>
        Więcej<svg xmlns="http://www.w3.org/2000/svg" width="20px" height="20px" viewBox="0 0 24 24"><path fill="none" stroke="#14AAA1" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m10 7l5 5l-5 5"/></svg>
    </a>
</div>

<style>
    .post-card {
        display: flex;
        flex-direction: column;
        justify-content: space-around;
        align-items: center;
        max-width: 344px;
        min-height: 431px;
        border-radius: 16px;
        padding: 16px;

        background-color: white;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);

        transition-duration: 0.5s;
    }
    .date {
        color: var(--gray);
        font-size: 1.4rem;
        font-weight: 400;
    }
    .title {
        color: var(--black);
        font-size: 2.2rem;
        font-weight: 500;
    }  

    .post-card:hover {
        transform-origin: top;
        transition-duration: 0.5s;
        box-shadow: 0 0 20px rgba(0, 0, 0, 0.4);
    }

    .title-post {
        display: flex;
        gap: 2em;
        align-items: center;
    }

    svg {
    vertical-align: middle;
    }
    h2 {
        font-size: 2rem;
    }
    a { 
        color: var(--black);
    }
    a:hover {
        color: var(--gray);
    }
    h3 {
        font-size: 2rem;
    }
    .description {
        font-size: 1.4rem;
        color: var(--gray);
    }

    .title h2 {
        font-size: 2.2rem;
    }

    .title {
        color: var(--black);
    }

    .title-date {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 1em;
    }

    .show-more {
        margin-left: auto;
        font-weight: 500;
        font-size: 1.4rem;
        color: var(--celadon);
    }
    
    img {
        border-radius: 8px;
        object-fit: cover;
        width: 100%;
    }

    .post-card:hover {
        transform-origin: top;
        transition-duration: 0.5s;
        box-shadow: 0 0 20px rgba(0, 0, 0, 0.4);
    }
    .post-card {
        transition-duration: 0.5s;
    }

</style>